name: private-drivers
version: 1.0.0

# Add your driver dependencies here.
# engine-driver and rwlock are required for compiling and testing.
dependencies:
  engine-driver:
    github: aca-labs/crystal-engine-driver
    branch: master

  rwlock:
    github: spider-gazelle/readers-writer

development_dependencies:
  ameba:
    github: veelenga/ameba
