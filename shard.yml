name: private-drivers
version: 1.0.0

# Add your driver dependencies here.
dependencies:
  # Required for compiling and testing ACAEngine drivers
  engine-driver:
    github: aca-labs/crystal-engine-driver
    branch: master

  # Feel free to remove this dependency. Used for testing,
  base62:
    github: Sija/base62.cr

development_dependencies:
  ameba:
    github: veelenga/ameba
